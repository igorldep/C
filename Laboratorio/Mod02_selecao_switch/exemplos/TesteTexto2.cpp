#include <stdio.h>
#include <stdlib.h>

int main (){

    char ch;

    printf ("\nDigite o caracter:");
    scanf("%c", &ch);

    printf ("\nCaracter digitado: %c", ch);

    printf ("\nFim do programa!!!");
    system("pause");
    return(0);
}

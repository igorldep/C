#include <stdio.h>
#include <stdlib.h>

/*Nome: Igor Luciano de Paula | Turma: LABCOMP1 SELE.1 - G1
 *Prof.: Geraldo Pereira de Souza | 1/2016
 *
 *Mod02_switch - Roteiro: Exercício N. 2:
 *Crie e compile o código fonte abaixo
 *a) Nome para o programa: ProgramaSoAceitaPositivo.c; b) Compile e execute o código fonte;
 *c) O que o programa faz?
 */

int main ()
{
    float num = 0;

    do
    {
        printf ("\nDigite um número inteiro e positivo: ");
        scanf("%f", &num);
    }
    while ( ( num < 0 ) || ( num == 0 ) );

    printf ("\nO número %f é inteiro e positivo!\n", num);
    printf ("\nFim do programa\n\n");

    system("pause");
return(0);
}

/*O programa recebe uma entrada e o armazena na variável inteira "num". Enquanto o numero for menor que zero
 *o programa repete a mensagem "Digite um número inteiro e positivo:", dando outra oportunidade para o usuário digitar
 *um número maior que 0. Quando o número dado pelo usuário for maior que zero o programa sai do loop, imprime na tela o número
 *e encerra o programa.
 */

